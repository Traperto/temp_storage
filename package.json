{
  "name": "temp_storage",
  "version": "0.6.1",
  "description": "A simple storage for temperature and humidity data which can be accessed via rest api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm i && tsc",
    "start": "if [ ! -d \"dist\" ]; then npm run-script compile\n fi\n node dist/index.js",
    "dev": "tsc && npm run-script start"
  },
  "author": "Niklas Schmidt",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4",
    "jsonwebtoken": "^8.3.0",
    "sqlite3": "^4.0.2",
    "typescript": "^3.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/sqlite3": "^3.1.3"
  }
}
